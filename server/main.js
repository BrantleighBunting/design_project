import { Meteor } from 'meteor/meteor';

// const ffi = require('ffi');

// I plan on calling rust from this method on the server if we
// stick with meteor, if not then the current setup is fine.

Meteor.startup(() => {
  // code to run on server at startup
});
